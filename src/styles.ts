import type { CSSProperties } from 'react'

export const colors = {
  bg: '#0c0c0f',
  surface: '#111116',
  surfaceElevated: '#16161d',
  surfaceHover: '#1c1c26',
  border: 'rgba(255,255,255,0.07)',
  borderStrong: 'rgba(255,255,255,0.12)',
  borderActive: '#6366f1',
  text: '#f0f0f2',
  textMuted: '#52526a',
  textSubtle: '#7878a0',
  accent: '#6366f1',
  accentHover: '#818cf8',
  accentSubtle: 'rgba(99,102,241,0.1)',
  accentBorder: 'rgba(99,102,241,0.25)',
  recording: '#f43f5e',
  recordingSubtle: 'rgba(244,63,94,0.08)',
  recordingBorder: 'rgba(244,63,94,0.2)',
  filled: 'rgba(99,102,241,0.05)',
  filledBorder: 'rgba(99,102,241,0.18)',
  warning: '#f59e0b',
  warningSubtle: 'rgba(245,158,11,0.06)',
  warningBorder: 'rgba(245,158,11,0.15)',
  error: '#f43f5e',
  errorSubtle: 'rgba(244,63,94,0.06)',
  errorBorder: 'rgba(244,63,94,0.18)',
}

export const styles = {
  container: {
    minHeight: '100vh',
    background: colors.bg,
    color: colors.text,
    fontFamily:
      '"Noto Sans JP", "Hiragino Kaku Gothic ProN", -apple-system, BlinkMacSystemFont, sans-serif',
  } satisfies CSSProperties,

  // ─── Header ───────────────────────────────────────────────
  header: {
    borderBottom: `1px solid ${colors.border}`,
    background: 'rgba(12,12,15,0.92)',
    backdropFilter: 'blur(12px)',
    position: 'sticky',
    top: 0,
    zIndex: 10,
  } satisfies CSSProperties,
  headerInner: {
    maxWidth: 1100,
    margin: '0 auto',
    padding: '0 24px',
    height: 48,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  } satisfies CSSProperties,
  logoArea: {
    display: 'flex',
    alignItems: 'center',
    gap: 12,
  } satisfies CSSProperties,
  logoMark: {
    width: 22,
    height: 22,
    borderRadius: 5,
    background: colors.accent,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: 11,
    fontWeight: 800,
    color: '#fff',
    letterSpacing: '-0.02em',
    flexShrink: 0,
  } satisfies CSSProperties,
  logoIcon: {
    width: 22,
    height: 22,
    borderRadius: 5,
    background: colors.accent,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: 11,
    fontWeight: 800,
    color: '#fff',
    letterSpacing: '-0.02em',
    flexShrink: 0,
  } satisfies CSSProperties,
  logoDivider: {
    width: 1,
    height: 16,
    background: colors.border,
  } satisfies CSSProperties,
  title: {
    margin: 0,
    fontSize: 13,
    fontWeight: 600,
    letterSpacing: '-0.01em',
    color: colors.text,
  } satisfies CSSProperties,
  subtitle: {
    display: 'none',
  } satisfies CSSProperties,
  badge: {
    display: 'flex',
    alignItems: 'center',
    gap: 6,
    fontSize: 10,
    color: colors.textMuted,
    fontFamily: 'ui-monospace, "JetBrains Mono", monospace',
    letterSpacing: '0.02em',
    padding: '4px 10px',
    borderRadius: 6,
    border: `1px solid ${colors.border}`,
    background: colors.surface,
  } satisfies CSSProperties,
  badgeDot: {
    width: 5,
    height: 5,
    borderRadius: '50%',
    background: '#22c55e',
    boxShadow: '0 0 6px #22c55e',
  } satisfies CSSProperties,

  // ─── Layout ───────────────────────────────────────────────
  main: {
    maxWidth: 1100,
    margin: '0 auto',
    padding: '20px 24px 60px',
    display: 'grid',
    gridTemplateColumns: '1fr 420px',
    gap: 16,
    alignItems: 'start',
  } satisfies CSSProperties,
  leftCol: {
    display: 'flex',
    flexDirection: 'column',
    gap: 12,
    minWidth: 0,
  } satisfies CSSProperties,
  rightCol: {
    display: 'flex',
    flexDirection: 'column',
    gap: 12,
    minWidth: 0,
  } satisfies CSSProperties,

  // ─── Banners ──────────────────────────────────────────────
  statusBar: {
    padding: '8px 12px',
    background: colors.accentSubtle,
    border: `1px solid ${colors.accentBorder}`,
    borderRadius: 6,
    fontSize: 12,
    color: colors.accentHover,
    fontWeight: 500,
    letterSpacing: '-0.01em',
  } satisfies CSSProperties,
  errorBar: {
    padding: '8px 12px',
    background: colors.errorSubtle,
    border: `1px solid ${colors.errorBorder}`,
    borderRadius: 6,
    fontSize: 12,
    color: colors.error,
    fontWeight: 500,
  } satisfies CSSProperties,
  warningBar: {
    padding: '8px 12px',
    background: colors.warningSubtle,
    border: `1px solid ${colors.warningBorder}`,
    borderRadius: 6,
    fontSize: 12,
    color: colors.warning,
    lineHeight: 1.55,
  } satisfies CSSProperties,

  // ─── Model Panel ──────────────────────────────────────────
  modelPanel: {
    padding: '14px 16px',
    background: colors.surface,
    border: `1px solid ${colors.border}`,
    borderRadius: 8,
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    flexWrap: 'wrap' as const,
  } satisfies CSSProperties,
  progressBarOuter: {
    flex: 1,
    minWidth: 140,
    height: 2,
    borderRadius: 1,
    background: colors.border,
    overflow: 'hidden',
  } satisfies CSSProperties,
  progressBarInner: {
    height: '100%',
    background: colors.accent,
    transition: 'width 0.3s ease',
  } satisfies CSSProperties,

  // ─── Controls ─────────────────────────────────────────────
  controlSection: {
    display: 'flex',
    flexDirection: 'column',
    gap: 0,
  } satisfies CSSProperties,
  controlGrid: {
    display: 'flex',
    gap: 8,
  } satisfies CSSProperties,
  btn: {
    display: 'inline-flex',
    alignItems: 'center',
    gap: 7,
    padding: '0 14px',
    height: 34,
    borderRadius: 6,
    border: '1px solid transparent',
    cursor: 'pointer',
    fontSize: 12,
    fontWeight: 500,
    transition: 'all 0.1s ease',
    position: 'relative',
    fontFamily: 'inherit',
    letterSpacing: '-0.01em',
    whiteSpace: 'nowrap' as const,
    flexShrink: 0,
  } satisfies CSSProperties,
  btnPrimary: {
    background: colors.accent,
    color: '#fff',
    border: `1px solid ${colors.accent}`,
  } satisfies CSSProperties,
  btnRecording: {
    background: colors.recording,
    color: '#fff',
    border: `1px solid ${colors.recording}`,
  } satisfies CSSProperties,
  btnAccent: {
    background: colors.accentSubtle,
    color: colors.accentHover,
    border: `1px solid ${colors.accentBorder}`,
  } satisfies CSSProperties,
  btnGhost: {
    background: 'transparent',
    color: colors.textSubtle,
    border: `1px solid ${colors.border}`,
  } satisfies CSSProperties,
  btnIcon: {
    fontSize: 11,
    lineHeight: 1,
    opacity: 0.7,
  } satisfies CSSProperties,
  pulse: {
    position: 'absolute',
    top: 7,
    right: 7,
    width: 5,
    height: 5,
    borderRadius: '50%',
    background: 'rgba(255,255,255,0.7)',
    animation: 'pulse-dot 1s ease-in-out infinite',
  } satisfies CSSProperties,

  // ─── Transcript ───────────────────────────────────────────
  transcriptSection: {
    background: colors.surface,
    borderRadius: 8,
    border: `1px solid ${colors.border}`,
    overflow: 'hidden',
  } satisfies CSSProperties,
  transcriptHeader: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    padding: '10px 14px',
    borderBottom: `1px solid ${colors.border}`,
  } satisfies CSSProperties,
  sectionTitle: {
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    fontSize: 11,
    fontWeight: 600,
    color: colors.textMuted,
    letterSpacing: '0.06em',
    textTransform: 'uppercase' as const,
    fontFamily: 'ui-monospace, monospace',
  } satisfies CSSProperties,
  charCount: {
    fontSize: 10,
    color: colors.textMuted,
    fontFamily: 'ui-monospace, monospace',
    letterSpacing: '0.02em',
  } satisfies CSSProperties,
  transcriptBody: {
    padding: '0 14px 14px',
  } satisfies CSSProperties,
  transcriptTextarea: {
    width: '100%',
    minHeight: 140,
    background: 'transparent',
    border: 'none',
    padding: '12px 0',
    color: colors.text,
    fontSize: 13,
    lineHeight: 1.75,
    outline: 'none',
    fontFamily: 'inherit',
    resize: 'vertical' as const,
    boxSizing: 'border-box' as const,
  } satisfies CSSProperties,
  transcriptTextareaRecording: {
    color: colors.text,
  } satisfies CSSProperties,
  liveBadge: {
    display: 'inline-flex',
    alignItems: 'center',
    gap: 5,
    fontSize: 9,
    fontWeight: 700,
    letterSpacing: '0.1em',
    color: colors.recording,
    background: colors.recordingSubtle,
    border: `1px solid ${colors.recordingBorder}`,
    padding: '2px 7px',
    borderRadius: 4,
    fontFamily: 'ui-monospace, monospace',
  } satisfies CSSProperties,
  liveDot: {
    width: 4,
    height: 4,
    borderRadius: '50%',
    background: colors.recording,
    animation: 'pulse-dot 1s ease-in-out infinite',
  } satisfies CSSProperties,
  transcriptText: {
    margin: 0,
    fontSize: 13,
    lineHeight: 1.8,
    color: colors.text,
  } satisfies CSSProperties,
  interimText: {
    color: colors.textSubtle,
  } satisfies CSSProperties,
  placeholder: {
    margin: 0,
    fontSize: 12,
    color: colors.textMuted,
  } satisfies CSSProperties,

  // Unused legacy (keep to avoid TS errors in App.tsx references)
  sectionToggle: { display: 'none' } satisfies CSSProperties,
  chevron: { display: 'none' } satisfies CSSProperties,

  // ─── Form ─────────────────────────────────────────────────
  formSection: {
    display: 'flex',
    flexDirection: 'column',
    gap: 1,
  } satisfies CSSProperties,
  formTitle: {
    fontSize: 11,
    fontWeight: 600,
    color: colors.textMuted,
    letterSpacing: '0.06em',
    textTransform: 'uppercase' as const,
    margin: '0 0 10px',
    fontFamily: 'ui-monospace, monospace',
  } satisfies CSSProperties,
  fieldGrid: {
    display: 'flex',
    flexDirection: 'column',
    gap: 1,
    background: colors.border,
    borderRadius: 8,
    overflow: 'hidden',
    border: `1px solid ${colors.border}`,
  } satisfies CSSProperties,
  fieldCard: {
    background: colors.surface,
    padding: '10px 14px',
    cursor: 'pointer',
    transition: 'background 0.1s ease',
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    minHeight: 44,
  } satisfies CSSProperties,
  fieldCardFull: {
    flexDirection: 'column' as const,
    alignItems: 'flex-start',
    minHeight: 60,
  } satisfies CSSProperties,
  fieldCardFilled: {
    background: colors.filled,
  } satisfies CSSProperties,
  fieldHeader: {
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    flexShrink: 0,
    width: 80,
  } satisfies CSSProperties,
  fieldIcon: {
    fontSize: 10,
    opacity: 0,
    width: 0,
  } satisfies CSSProperties,
  fieldLabel: {
    fontSize: 11,
    fontWeight: 500,
    color: colors.textMuted,
    letterSpacing: '-0.01em',
    whiteSpace: 'nowrap' as const,
  } satisfies CSSProperties,
  filledDot: {
    width: 4,
    height: 4,
    borderRadius: '50%',
    background: colors.accent,
    flexShrink: 0,
  } satisfies CSSProperties,
  fieldValue: {
    margin: 0,
    fontSize: 13,
    fontWeight: 400,
    color: colors.text,
    lineHeight: 1.5,
    flex: 1,
    letterSpacing: '-0.01em',
  } satisfies CSSProperties,
  fieldPlaceholder: {
    margin: 0,
    fontSize: 12,
    color: colors.textMuted,
    flex: 1,
    letterSpacing: '-0.005em',
  } satisfies CSSProperties,
  fieldInput: {
    flex: 1,
    background: 'transparent',
    border: 'none',
    borderBottom: `1px solid ${colors.borderActive}`,
    padding: '2px 0',
    color: colors.text,
    fontSize: 13,
    outline: 'none',
    fontFamily: 'inherit',
    boxSizing: 'border-box' as const,
    letterSpacing: '-0.01em',
  } satisfies CSSProperties,
  fieldTextarea: {
    width: '100%',
    background: 'transparent',
    border: 'none',
    borderBottom: `1px solid ${colors.borderActive}`,
    padding: '4px 0',
    color: colors.text,
    fontSize: 13,
    outline: 'none',
    fontFamily: 'inherit',
    resize: 'vertical' as const,
    boxSizing: 'border-box' as const,
    lineHeight: 1.6,
    marginTop: 4,
  } satisfies CSSProperties,

  // ─── Architecture ─────────────────────────────────────────
  archSection: {
    background: colors.surface,
    borderRadius: 8,
    border: `1px solid ${colors.border}`,
    padding: '14px 16px',
  } satisfies CSSProperties,
  archTitle: {
    margin: '0 0 12px',
    fontSize: 10,
    fontWeight: 600,
    color: colors.textMuted,
    textTransform: 'uppercase' as const,
    letterSpacing: '0.1em',
    fontFamily: 'ui-monospace, monospace',
  } satisfies CSSProperties,
  archFlow: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 6,
    marginBottom: 14,
    flexWrap: 'wrap' as const,
  } satisfies CSSProperties,
  archStep: {
    textAlign: 'center' as const,
    padding: '8px 10px',
    background: colors.surfaceElevated,
    borderRadius: 6,
    border: `1px solid ${colors.border}`,
    minWidth: 80,
  } satisfies CSSProperties,
  archStepIcon: {
    fontSize: 11,
    marginBottom: 4,
    color: colors.accent,
    fontFamily: 'ui-monospace, monospace',
  } satisfies CSSProperties,
  archStepLabel: {
    fontSize: 11,
    fontWeight: 600,
    marginBottom: 2,
    color: colors.text,
    letterSpacing: '-0.01em',
  } satisfies CSSProperties,
  archStepTech: {
    fontSize: 9,
    color: colors.textMuted,
    fontFamily: 'ui-monospace, monospace',
    letterSpacing: '0.02em',
  } satisfies CSSProperties,
  archStepDesc: {
    fontSize: 9,
    color: colors.textMuted,
    marginTop: 2,
  } satisfies CSSProperties,
  archArrow: {
    fontSize: 12,
    color: colors.textMuted,
    opacity: 0.5,
  } satisfies CSSProperties,
  archNote: {
    background: colors.surfaceElevated,
    border: `1px solid ${colors.border}`,
    borderRadius: 6,
    padding: '10px 12px',
    fontSize: 12,
    color: colors.textSubtle,
    lineHeight: 1.6,
  } satisfies CSSProperties,
  code: {
    background: 'rgba(255,255,255,0.06)',
    padding: '1px 5px',
    borderRadius: 3,
    fontSize: 10,
    fontFamily: 'ui-monospace, monospace',
    color: colors.accentHover,
  } satisfies CSSProperties,
}
